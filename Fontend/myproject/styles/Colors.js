const COLORS = {
    // ─── Primary (Xanh dương y tế) ───────────────────────
    primary: "#1976D2",          // Xanh đậm - màu chủ đạo
    primaryDark: "#1565C0",      // Hover / pressed
    primaryLight: "#E3F2FD",     // Background nhạt
    primaryBorder: "#90CAF9",    // Border / outline

    // ─── Secondary (Xanh lam nhẹ) ────────────────────────
    secondary: "#0EA5E9",        // Accent, tag, badge
    secondaryLight: "#E0F2FE",   // Background phụ

    // ─── Semantic ─────────────────────────────────────────
    success: "#16A34A",          // Đã khám / Hoàn thành
    successLight: "#DCFCE7",
    successBorder: "#86EFAC",

    warning: "#D97706",          // Chờ xác nhận / Cảnh báo
    warningLight: "#FEF3C7",
    warningBorder: "#FCD34D",

    error: "#DC2626",            // Hủy / Từ chối / Lỗi
    errorLight: "#FEE2E2",
    errorBorder: "#FCA5A5",

    info: "#0284C7",             // Thông báo / Hướng dẫn
    infoLight: "#E0F2FE",
    infoBorder: "#7DD3FC",

    // ─── Trạng thái lịch hẹn ─────────────────────────────
    statusPending: "#D97706",    // Chờ xác nhận
    statusConfirmed: "#1976D2",  // Đã xác nhận
    statusDone: "#16A34A",       // Hoàn thành
    statusCancelled: "#DC2626",  // Đã hủy

    // ─── Text ─────────────────────────────────────────────
    text: "#0F172A",             // Chữ chính
    textSecondary: "#374151",    // Chữ phụ
    textMuted: "#6B7280",        // Placeholder / nhãn mờ
    textLight: "#9CA3AF",        // Disabled text
    textInverse: "#FFFFFF",      // Chữ trên nền tối

    // ─── Background ───────────────────────────────────────
    bg: "#F0F4F8",               // Nền app (xanh xám nhẹ - cảm giác y tế)
    bgCard: "#FFFFFF",           // Nền card
    bgInput: "#F8FAFC",          // Nền input

    // ─── Neutral ──────────────────────────────────────────
    white: "#FFFFFF",
    border: "#E2E8F0",
    divider: "#F1F5F9",

    // ─── Button ───────────────────────────────────────────
    btnPrimary: "#1976D2",
    btnDanger: "#DC2626",
    btnWarning: "#D97706",
    btnDisabled: "#CBD5E1",
};

export default COLORS;